services:
  api:
    build: .
    command: sh -c "[ -f db/chroma.sqlite3 ] || python criar_db.py && uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./base:/app/base
      - ./db:/app/db
    restart: unless-stopped
